#include <bits/stdc++.h>

using namespace std;

int main() {

    int t;
    cin >> t;
    while (t--) {
        int a, b, c, x, y, z;
        cin >> a >> b >> c >> x >> y >> z;
        // Calulating points per min of each type question.
        double ppmA = double(a)/double(x);
        double ppmB = double(b)/double(y);
        double ppmC = double(c)/double(z);
        
        // cout<<ppmA<<ppmB<<ppmC;
        int totalTime = 240;
        if(ppmA>=ppmB && ppmA>=ppmC){
            
        }
        
     
























    }


    return 0;
}